{
  "name": "smart-results-finder",
  "version": "0.1.0",
  "private": true,
  "description": "Controlled examination-results discovery MVP",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
